# Requirements

## 1. Project Goal

构建一个面向 A 股日频研究的本地量化系统，支持：

- 稳定积累股票和 ETF 日线数据
- 生成每日推荐与数据状态摘要
- 运行策略实验与参数扫描
- 执行横截面回测并输出绩效指标

系统定位为研究型工具，不是实盘交易系统。

## 2. Target User

- 个人量化研究者
- 本地运行脚本与分析任务的开发者
- 需要将代码托管到远端仓库并持续迭代的人

## 3. In Scope

### 3.1 Data

- 沪深市场日频行情
- 场内 ETF 日频行情
- 股票池、ETF 池、持仓文件、本地元数据维护
- 全量历史回补
- 日常快速更新
- 数据覆盖与字段完整性盘点

### 3.2 Strategy

- 基于本地日频数据构建策略快照
- 运行默认动量策略
- 输出每日推荐列表
- 允许策略参数配置化
- 支持参数扫描并保存比较结果

### 3.3 Backtest

- 日频横截面回测
- 等权选股组合
- 成本建模
- 输出收益曲线、选股记录和绩效摘要

### 3.4 Operations

- 命令行统一入口
- 日更脚本
- 工作日自动调度
- 日志输出
- 状态摘要与库存统计文件
- 本地只读 dashboard 浏览入口

## 4. Out of Scope

- 分钟级/高频数据
- 实盘下单与交易执行
- 账户管理、风控审批、订单路由
- 多用户或生产化 Web 服务

## 5. Functional Requirements

### FR-1 数据初始化

系统必须支持初始化本地目录、SQLite 元数据库和持仓模板。

### FR-2 全量历史回补

系统必须支持按资产类型回补 2000 年以来的股票和 ETF 历史数据。

### FR-3 快速日更

系统必须支持只更新“落后于本地最新交易日”的已有标的，并允许并发抓取。

### FR-4 股票池管理

系统必须支持刷新本地 universe，并保存股票/ETF 的基本元信息。

### FR-5 数据盘点

系统必须输出逐标的数据盘点表和汇总表，至少包含：

- 标的代码
- 资产类型
- 市场
- 记录数
- 起止日期
- 字段列表
- 空值统计
- 最近更新时间

### FR-6 策略实验

系统必须支持根据最新本地数据生成策略快照，并输出候选排名。

### FR-7 推荐与持仓检查

系统必须输出每日推荐结果和持仓检查结果。

### FR-8 回测

系统必须支持在全量本地数据上执行横截面日频回测，并输出：

- 逐日收益
- 净值曲线
- 每日选股记录
- 绩效摘要

### FR-9 参数扫描

系统必须支持基于配置网格批量扫描策略参数，并输出比较结果表。

### FR-10 状态摘要

系统必须支持生成便于人工查看的每日状态摘要文件。

### FR-11 更新失败追踪

系统必须输出更新失败代码清单，并支持对失败代码进行定向重试。

### FR-12 本地浏览入口

系统必须支持生成本地 HTML dashboard，并可通过本地 HTTP 服务浏览当前数据、推荐、回测和参数扫描结果。

## 6. Non-Functional Requirements

### NFR-1 本地优先

系统必须以本地文件和本地 SQLite 为主，不依赖远程数据库。

### NFR-2 可恢复

长任务中断后，系统应尽量允许从已有文件继续运行，而不是重新全量下载。

### NFR-3 可追踪

所有更新任务必须记录日志或元数据，至少包括：

- 执行时间
- 更新范围
- 下载标的数
- 写入行数

### NFR-4 可配置

核心策略参数、回测成本参数和参数扫描网格必须可配置。

### NFR-5 仓库友好

仓库必须只提交代码、模板配置和文档；大体量数据和结果默认不提交远端库。

## 7. Current Known Limitations

- 深市创业板 `300/301` 股票池尚未完整接入
- 深市部分股票池仍依赖后续数据源补齐
- 参数扫描在全市场全历史下仍是长任务
- 当前策略可运行，但尚未证明具备实用投资表现

## 8. Acceptance Criteria

满足以下条件视为当前版本可交付：

- `python3 run.py fast_update --workers 16 --lookback-days 7` 能执行并写入最新数据
- `python3 run.py status` 能生成 `outputs/daily_status.md`
- `python3 run.py experiment` 能输出推荐候选
- `python3 run.py backtest` 能输出回测结果和 `outputs/backtest_summary.csv`
- `python3 run.py sweep` 能输出 `outputs/strategy_sweep.csv`
- README 和 REQUIREMENTS 可直接用于远端仓库说明
- 新环境可按 README 完成依赖安装并启动 CLI
- `python3 run.py dashboard` 能生成本地 dashboard
